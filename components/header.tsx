import { FC } from "react";

const Header: FC = () => {
  return (
    <div className="w-screen h-[50px] fixed bg-white bg-opacity-10 flex items-center font-bold text-xl px-[15px]">
      framer-option-work
    </div>
  );
};

export default Header;
